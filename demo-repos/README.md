# Demo Repositories

The MVP includes a Bob-powered demo route that sends a representative legacy cloud API scan context to the backend Bob adapter.

The placeholder file keeps the demo repository directory present without bundling a large sample application.
